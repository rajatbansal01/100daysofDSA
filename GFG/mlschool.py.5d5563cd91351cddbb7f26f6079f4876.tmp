'''
Robots with strings
Ray, Ben and Kevin are three robots.
Ray has a string S of length n, Ben and Kevin has empty strings.
Allowed Moves: 
We can append first character of Ray's string to Ben's string. 
We can append last character of Ben's string to Kevin's string.
Perform moves in such a way that Ray and Ben have empty strings and Kevin has lexographically smallest string.
return that lexographically smallest string.
'''

def robots_with_strings(n, S):
    
    
    